export const API_ENDPOINT = 'http://localhost:5082/api';

// Notes Endpoint URLs
export const NOTES_ENDPOINT = `${API_ENDPOINT}/notes`;
// you need to send id along with the request in the below API endpoint
export const GET_NOTES_BY_ID_ENDPOINT = `${NOTES_ENDPOINT}/`;
